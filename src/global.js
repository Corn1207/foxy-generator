let totalImages = 0;
let loadedImages = 0;

const report = () => {
  console.log(`
  -----------------------------------
  🆘 Total imágenes: ${totalImages}

  ✅ Imágenes cargadas: ${loadedImages}
  -----------------------------------
  `);
};
